# Neko Browser App

A standalone, dependency-free browser preview of Neko at the sprite's native
192 × 208 pixel frame size. The pet is never CSS-scaled.

Open `index.html` directly in a modern browser, or serve the folder with any
static web server.
